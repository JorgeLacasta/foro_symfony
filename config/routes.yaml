index:
    path: /
    controller: Symfony\Bundle\FrameworkBundle\Controller\RedirectController::urlRedirectAction
    defaults:
        path: /ultimas
        permanent: true

#Ruta por defecto a la raiz, eliges un controllador y su funcion